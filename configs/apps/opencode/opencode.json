{
  "$schema": "https://opencode.ai/config.json",
  "keybinds": {
    "input_backspace": "ctrl+h,shift+backspace",
    "input_delete_word_backward": "ctrl+w,ctrl+backspace,alt+backspace,option+backspace,escape"
  }
}